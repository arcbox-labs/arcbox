"""arcbox — run isolated microVM sandboxes on the local ArcBox daemon.

Public surface only: everything under `arcbox._gen` is generated wire
code and is deliberately NOT exported — public shapes are hand-written
and mapped at the transport boundary.
"""
